import React from "react";
import HomeScreen from "../tabs/HomeScreen";

// For now reuse your existing HomeScreen UI
export default function PatientHomeScreen(props) {
  return <HomeScreen {...props} />;
}
